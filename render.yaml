services:
  - type: web
    name: orbitbharat-api
    runtime: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: cd api && uvicorn server:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: PYTHON_VERSION
        value: "3.11.0"
