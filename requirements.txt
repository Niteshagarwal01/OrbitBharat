# Requirements for OrbitBharat API deployment
# Lighter than local ml_pipeline/requirements.txt — no GPU, no viz libs

# Core ML (CPU-only torch for free tier)
--extra-index-url https://download.pytorch.org/whl/cpu
torch>=2.0.0
numpy>=1.24.0
pandas>=2.0.0

# API Server
fastapi>=0.100.0
uvicorn[standard]>=0.22.0
pydantic>=2.0.0

# Data & Networking
requests>=2.28.0
aiohttp>=3.8.0

# Utilities
python-dotenv>=1.0.0
